{
    "name": "root",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "@truffle/hdwallet-provider": "^2.0.0",
        "dotenv": "^8.2.0",
        "gh-pages": "^3.2.3"
    },
    "scripts": {
        "predeploy": "npm run build --prefix client",
        "deploy": "gh-pages -d client/build"
    }
}
